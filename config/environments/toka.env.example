# Toka OS Main Environment Template
# Copy this file to .env.local and customize for your setup
# This is the comprehensive template for all Toka OS components

# =============================================================================
# LLM Provider Configuration (Choose ONE)
# =============================================================================

# Anthropic Claude (Recommended)
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI GPT-4 (Alternative)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=your_openai_api_key_here

# LLM Configuration
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_RATE_LIMIT=50
LLM_TIMEOUT=30
LLM_DEBUG=false

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite (Development)
DATABASE_URL=sqlite:///app/data/toka.db
STORAGE_TYPE=sqlite

# PostgreSQL (Production)
# DATABASE_URL=postgresql://user:password@localhost/toka
# POSTGRES_DB=toka
# POSTGRES_USER=toka
# POSTGRES_PASSWORD=toka_dev_password_change_in_production

# Redis (Caching)
# REDIS_PASSWORD=toka_redis_password_change_in_production
# REDIS_URL=redis://localhost:6379

# =============================================================================
# Agent Configuration
# =============================================================================

# Agent Pool Settings
AGENT_POOL_SIZE=10
MAX_CONCURRENT_AGENTS=8
AGENT_SPAWN_TIMEOUT=30
AGENT_WORKSTREAM_TIMEOUT=3600

# Agent Runtime Settings
AGENT_SANDBOX_ENABLED=true
CAPABILITY_VALIDATION=strict
AGENT_DEV_MODE=true
AGENT_DEBUG_ENABLED=true

# =============================================================================
# Security Settings
# =============================================================================

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Security Features
RUST_SECURE_ZERO_MEMORY=1
ENABLE_SECURITY_SCANNING=true
SECURITY_SCAN_INTERVAL=3600

# =============================================================================
# Performance Settings
# =============================================================================

# Rust Settings
RUST_LOG=info
RUST_BACKTRACE=1
RUSTFLAGS=-D warnings

# Tokio Runtime
TOKIO_WORKER_THREADS=4

# Caching Configuration
CONTEXT_CACHE_SIZE=2048
CONTEXT_CACHE_TTL=7200
RESPONSE_CACHE_SIZE=1024
RESPONSE_CACHE_TTL=3600

# Build Performance
CARGO_INCREMENTAL=1
CARGO_NET_RETRY=10
CARGO_BUILD_JOBS=4
MAKEFLAGS=-j4

# =============================================================================
# GitHub Integration (Optional)
# =============================================================================

# GitHub OAuth (for collaborative features)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret
# GITHUB_OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# GitHub Repository
# GITHUB_TOKEN=your_github_token_here
# GITHUB_OWNER=your-org
# GITHUB_REPO=your-repo

# =============================================================================
# Monitoring & Observability
# =============================================================================

# Metrics Collection
METRICS_ENABLED=true
METRICS_INTERVAL=30
METRICS_PORT=9090

# Tracing & Logging
TRACING_ENABLED=true
LOG_LEVEL=info

# Grafana Dashboard
GRAFANA_PASSWORD=toka_grafana_password_change_in_production

# Prometheus Settings
PROMETHEUS_RETENTION=200h

# Jaeger Tracing
JAEGER_AGENT_HOST=jaeger
JAEGER_AGENT_PORT=6831

# =============================================================================
# Environment Configuration
# =============================================================================

# Environment Type
TOKA_ENV=development
TOKA_LOG_LEVEL=debug

# Development Directories
AGENT_DATA_DIR=/app/data
AGENT_LOG_DIR=/app/logs
AGENT_CONFIG_DIR=/app/config

# =============================================================================
# Orchestration Settings
# =============================================================================

# Orchestration Engine
ORCHESTRATION_SESSION_TIMEOUT=7200
ORCHESTRATION_MAX_RETRIES=3
ORCHESTRATION_RETRY_DELAY=5

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30
HEALTH_CHECK_TIMEOUT=10
HEALTH_CHECK_RETRIES=3

# =============================================================================
# Container Configuration
# =============================================================================

# Container Resource Limits
CONTAINER_MEMORY_LIMIT=2g
CONTAINER_CPU_LIMIT=2

# Volume Paths (optional customization)
# CARGO_CACHE_PATH=/var/cache/cargo
# TARGET_CACHE_PATH=/var/cache/target

# =============================================================================
# Port Configuration
# =============================================================================

# Core Services
TOKA_API_PORT=8080
AGENT_MGMT_PORT=9000
WEBSOCKET_PORT=9001

# Development Services
FRONTEND_PORT=3000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
JAEGER_PORT=16686

# Database Services
REDIS_PORT=6379
POSTGRES_PORT=5432

# =============================================================================
# Testing Configuration
# =============================================================================

# Test Settings
ENABLE_FUZZING=true
FUZZ_TIMEOUT=300
ENABLE_PROFILING=false
PROFILING_INTERVAL=300

# Debug Settings
DEBUG_MODE=true
VERBOSE_LOGGING=true
ENABLE_GDB=true
ENABLE_VALGRIND=false

# =============================================================================
# Advanced Configuration
# =============================================================================

# Experimental Features
ENABLE_EXPERIMENTAL=false
EXPERIMENTAL_FEATURES=

# Custom Tool Paths
# CUSTOM_TOOL_PATH=/app/custom-tools
# CUSTOM_MANIFEST_PATH=/app/custom-manifests

# =============================================================================
# Production Overrides (for production deployments)
# =============================================================================

# Uncomment and set for production:
# TOKA_ENV=production
# TOKA_LOG_LEVEL=info
# DEBUG_MODE=false
# RUST_BACKTRACE=0
# AGENT_SANDBOX_ENABLED=true
# CAPABILITY_VALIDATION=strict
# ENABLE_SECURITY_SCANNING=true 