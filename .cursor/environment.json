{
  "name": "Toka Cursor Agent Environment",
  "user": "toka",
  "install": "cargo build --release --locked && cargo audit",
  "start": "/app/bin/toka-cli orchestrate --config /app/config/cursor-agents.toml --cursor-mode",
  "container": {
    "build": {
      "dockerfile": "Dockerfile",
      "context": ".."
    },
    "agentCanUpdateSnapshot": true
  },
  "repositoryDependencies": [
    "github.com/ScrappyAI/toka"
  ],
  "ports": [
    {
      "name": "Cursor Agent API",
      "port": 8080
    },
    {
      "name": "Cursor Agent Management",
      "port": 9000
    },
    {
      "name": "Cursor Agent WebSocket",
      "port": 9001
    },
    {
      "name": "Health Check",
      "port": 3000
    }
  ],
  "terminals": [
    [
      {
        "name": "Cursor Agent Orchestrator",
        "command": "/app/bin/toka-cli orchestrate --config /app/config/cursor-agents.toml --cursor-mode --verbose",
        "description": "Main cursor agent orchestration service with real-time logging"
      },
      {
        "name": "Agent Status Monitor",
        "command": "/app/bin/toka-cli monitor --cursor-agents --real-time",
        "description": "Real-time monitoring of cursor background agents and their execution status"
      }
    ],
    {
      "name": "Cursor Agent Logs",
      "command": "tail -f /app/logs/cursor-agents.log",
      "description": "Live tail of cursor agent execution logs and debugging information"
    },
    {
      "name": "Performance Metrics",
      "command": "/app/bin/toka-cli metrics --cursor-agents --export-prometheus",
      "description": "Real-time performance metrics for cursor agent optimization and resource usage"
    },
    {
      "name": "Agent Interactive Shell",
      "command": "/app/bin/toka-cli shell --cursor-mode",
      "description": "Interactive shell for managing cursor agents and executing ad-hoc commands"
    }
  ]
}